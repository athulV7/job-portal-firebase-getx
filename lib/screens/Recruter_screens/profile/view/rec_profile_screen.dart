import 'package:flutter/material.dart';

class RecProfileScreen extends StatelessWidget {
  const RecProfileScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
